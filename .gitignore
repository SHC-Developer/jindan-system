node_modules/

# 빌드 산출물은 제외하지만, NSIS 스크립트 등 일부 설정 파일은 예외 처리
build/
!build/installer.nsh

dist/
dist-electron/
release/
coverage/
.DS_Store
*.log

# 환경 변수 (비밀 키 포함) — 절대 커밋하지 않음
.env
.env.local
.env.*.local
!.env.example

# Firebase 서비스 계정 키 (스크립트용, 절대 커밋 금지)
firebase-admin-key.json
firebase-admin-key*.json
